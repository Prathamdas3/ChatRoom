interface Card {
  title: string;
  des: string;
}
interface AuthProp {
  isUser: boolean;
  setIsUser: any;
}

// interface Message {
//   text: string;
//   createdAt: () => void;
// }

interface ChatProp {
  code: string;
}
